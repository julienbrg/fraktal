import React from "react";
import styled from "styled-components";

const Title = styled.h4`
  font-weight: 600;
  font-size: 24px;
  text-align: right;
  margin: 60px;
`;

export default props => <Title>Fraktal app</Title>;
